VITE_API_URL=http://localhost:4000

# Google OAuth Client ID for "Sign in with Google". Leave empty to hide the
# Google Sign-In button entirely (it renders nothing until this is set).
VITE_GOOGLE_CLIENT_ID=
